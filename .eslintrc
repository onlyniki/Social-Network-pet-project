{
  "extends": [
    "react-app",
    "react-app/jest"
  ],
  "rules": {
    "no-unused-vars": "warn",
    "no-console":"warn",
    "prefer-const":"warn",
    "max-len": ["warn", {"code": 120}],
    "comma-dangle":["warn", "always-multiline"],
    "semi": ["warn", "always"]

  }
}